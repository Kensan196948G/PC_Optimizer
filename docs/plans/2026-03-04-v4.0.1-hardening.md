# v4.0.1 Hardening 着手メモ

作成日: 2026-03-04  
ブランチ: `feature/v4.0.1-hardening`

## 目的

v4.0.0 の CLI 運用を安定化し、入力バリデーションと実行エラーの診断性を向上する。

## 初期スコープ

1. `-Tasks` 異常系の拡張（空白混在、重複範囲、上限超過）
2. `-ExportDeletedPathsPath` のパス妥当性判定を詳細化（相対/絶対、予約語、無効文字）
3. `-FailureMode` と終了コードの整合テスト追加
4. JSONレポートの異常系出力（`status=NG/PARTIAL`）検証拡張
5. CI 失敗時 artifact への診断メタ情報追加

## PR分割案

- PR-A: CLI入力バリデーション追加
- PR-B: JSONレポート異常系とスキーマ拡張
- PR-C: CI診断ログ改善

## 完了条件

- 追加テストが PS5.1 / PS7 で PASS
- `master` の required checks をすべて満たす
- `docs/使い方.md` と `docs/config仕様.md` の差分を同期
