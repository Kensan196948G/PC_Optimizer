# v3.3.1 リリースノート（ドラフト）

リリース日: TBD
対象タグ: `v3.3.1`

## 概要
- テスト保守性向上（文言依存の低減・データ駆動化）と運用改善（CI証跡強化、保守ルール明確化）を中心に更新。

## 変更点
- Added:
  - CI でテストログを artifact 保存（PS5.1 / PS7）
  - 次版運用用の `v3.3.1` リリースノートドラフトを追加
- Changed:
  - `Test_Encoding.ps1` のブラウザ検証を機能要件ベースへ変更
  - `tests/Test_PCOptimizer.ps1` Task20 検証をデータ駆動化
  - CI artifact の保存期間を `14日` に設定
- Fixed:
  - 文言の軽微変更でテストが不要に失敗するリスクを低減

## 既知制約
- 管理者権限での実行が必須
- 一部タスクは対話入力（Y/N）を必要とする

## 検証結果
- `tests/Test_PCOptimizer.ps1`: 93/93 PASS（master）
- `Test_Encoding.ps1`: 96/96 PASS
- PowerShell 7.x 互換性: CI ジョブ `Test (PowerShell 7)` PASS

## 参考
- 比較元タグ: `v3.3.0`
- 関連PR:
  - #2 `test/docs: 文言依存低減と運用文書更新（v3.3.1準備）`（MERGED）
  - #3 `ci/docs: artifact保存と運用文書更新（v3.3.1）`（MERGED）
  - #5 `test: Task20テストのデータ駆動化着手`（MERGED）
